# Server environment configuration (copy to server/.env for local dev)
NODE_ENV=development
PORT=5000

# PostgreSQL connection for the "postgresql-adjacent-16876" database.
# Consumed by Prisma via datasource db.url = env("DATABASE_URL").
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/postgresql-adjacent-16876?schema=public"
